import { useEffect } from "react";
import React from "react";

interface WebpagesProps {
  RouteLocation: Location;
}

export const Webpages = (props: WebpagesProps) => {
  useEffect(() => {}, []);

  return <h1>Webpages</h1>;
};
